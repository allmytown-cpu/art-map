# PROJECT.md — 제작 기록

ART MAP이 왜 지금 구조가 되었는지, 어떤 것을 시도했고 무엇이 틀렸는지 적어둔 문서.
**"왜 이렇게 만들었나"가 궁금할 때** 읽는다.
코드를 고치기 전 실무 정보는 [`AGENTS.md`](AGENTS.md)에 있다.

- 제작: 2026-09-13
- 저장소: `allmytown-cpu/art-map`
- 배포: https://allmytown-cpu.github.io/art-map/

---

## 1. 목표

> 미술전시 정보를 지도에 매핑. 공공데이터를 끌어와 네이버 지도에 올리고,
> 사용자 GPS 기준으로 가장 가까운 정보를 보여준다. 커뮤니티 매핑과 유사한 형태.

이전 프로젝트(안양 커뮤니티 매핑)와 같은 방식 — GitHub + 네이버 지도 API + 정적 사이트.

---

## 2. 핵심 설계 결정

### 2.1 왜 서버가 없나

공공데이터포털 API는 브라우저에서 직접 호출할 수 없다.

- CORS 헤더가 없어 `fetch`가 차단됨
- 서비스키가 URL에 들어가므로 프론트엔드에 넣으면 도난당함
- 응답이 XML이고 좌표가 비어 있는 레코드가 많아 전처리가 필요함

**해결: GitHub Actions를 서버 대신 쓴다.**
Actions가 주기적으로 API를 호출해 JSON으로 가공한 뒤 저장소에 커밋하고,
GitHub Pages가 그 커밋을 감지해 자동 재배포한다.

| 항목 | 결과 |
|---|---|
| 서버·DB 비용 | 0원 |
| 서비스키 노출 | 없음 (GitHub Secrets) |
| 사용자 체감 속도 | API 실시간 호출보다 빠름 (정적 JSON) |
| Actions 요금 | 공개 저장소는 무제한 무료 |

### 2.2 다중 소스 + 정규화 스키마

프론트엔드는 `data/events.json` **하나만** 읽는다. 어느 API에서 왔는지 모른다.
덕분에 소스를 추가할 때 수집 스크립트만 만들면 지도·필터·거리정렬이 그대로 동작한다.

```
sources/kcisa.json  ┐
sources/artmap.json ├→ build-events.mjs →  data/events.json
sources/manual.json ┘   (병합·중복제거)
```

### 2.3 왜 art-map.co.kr을 매일 긁지 않나

전수 크롤링해서 실측한 결과:

```
art-map.co.kr 진행중 전시   23건
  ├─ 공공데이터와 중복      10건
  └─ 신규                 13건   ← 150회 요청의 순수익
```

매일 150회씩 남의 서버를 긁어 13건을 얻는 건 비용 대비 효과가 나쁘고,
상업 사이트를 매일 크롤링하는 것도 부담스럽다. **주 1회 + 수동 실행**으로 결정.

다만 그 13건의 질이 좋다 — 스페이스K, 아라리오갤러리, 뮤지엄산, 대구간송미술관 등
공공데이터가 놓치는 사립·상업 갤러리다.

### 2.4 링크 입력 방식

> "내가 일일이 수동으로 넣는다면 안하게 될 거 같아."

그래서 **URL만 붙여넣으면 정보를 추출해 핀을 꽂는** 방식으로 만들었다.
정적 사이트라 서버가 없으므로, 관리자 페이지가 브라우저에서 GitHub API를 직접 호출해
워크플로를 실행시킨다. 토큰은 저장소 Actions 권한만 가진 fine-grained PAT를 쓰고
localStorage에만 보관한다.

---

## 3. 겪은 문제와 해결

실제로 시간을 잡아먹은 것들. 같은 곳을 다시 밟지 않기 위해 기록한다.

### 3.1 공공데이터 API 공식 문서가 통째로 틀림

culture.go.kr 개발가이드대로 짠 첫 구현은 **전부 404**였다.
실호출로 하나씩 확인한 결과:

| 항목 | 문서 | 실제 |
|---|---|---|
| Base URL | `~/nopenapi/rest/publicperformancedisplays` | `~/cultureinfo` |
| 오퍼레이션 | `period`, `area`, `realm`, `detail` | **`2`가 붙은 것만 생존** |
| 페이지 번호 | `cPage` | **`PageNo`** (대문자 P) |
| 페이지 크기 | `rows` | **10 고정, `rows` 무시** |
| `from`/`to` | (미기재) | **`startDate`가 아니라 `endDate` 기준** |

마지막 항목 때문에 초기의 "월 단위 분할 수집"은 근본적으로 잘못된 접근이었다.
올바른 방식은 `from=오늘, to=먼 미래`로 **한 번에** 조회하는 것.

> **교훈**: 문서를 믿지 말고 실호출로 검증한 뒤 코드를 짠다.
> 검증 없이 배포했다면 데이터가 잘못 들어간 걸 한참 뒤에 발견했을 것이다.

### 3.2 지도가 안 뜸 — 원인은 CSS 한 줄

가장 많은 시간을 낭비한 문제. NCP 도메인 설정을 세 번이나 의심했지만 전부 헛다리였다.

앱에 진단 패널을 넣자 즉시 답이 나왔다.

```
map 크기      1920 x 0          ← 컨테이너 높이가 0
tile <img>    39개 (로드 성공 39개)   ← 타일은 전부 정상
authFailure   발생 안 함
```

**네이버 지도가 초기화하면서 컨테이너의 `position`을 인라인으로 덮어쓴다.**
그러면 `position:absolute; inset:0`으로 크기를 잡던 방식이 무력화되고,
자식 타일은 전부 절대 위치라 높이에 기여하지 않아 **높이가 0으로 무너진다.**
타일 39장을 멀쩡히 받아놓고 높이 0짜리 상자에 그리고 있었던 것.

→ `height: 100dvh`로 못박아 해결.

> **교훈**: 추측으로 세 번 고치는 것보다 상태를 화면에 출력하는 게 빠르다.
> 진단 패널은 `?debug=1`로 남겨뒀다.

### 3.3 서비스 워커가 수정을 삼킴

지도 버그를 고치는 동안 세 번의 수정이 사용자 화면에 반영되지 않았다.
PWA 서비스 워커가 CSS/JS를 **캐시 우선**으로 돌려주고 있었고,
`Ctrl+Shift+R`로도 서브리소스까지 확실히 우회되지 않았다.

→ 규칙을 바꿔 복구:
- HTML·데이터는 **네트워크 우선**
- CSS/JS는 `index.html`에서 `?v=N`으로 참조 → URL 자체가 버전
- 비상 탈출구 `?nosw=1`

### 3.4 중복 제거가 멀쩡한 전시를 삭제

초안 기준(제목 유사도 0.6 + 같은 장소)으로 **9건이 잘못 병합**됐다.

```
✗ 지브리 콘서트 "바다와 함께하는 여름"  ←→  "바람계곡의 나우시카"
✗ 릴레이 프로젝트 《빈칸으로…》        ←→  《망각의 저장고》
✗ [2026 PLOT STAGE] 유지원 <Wharton>  ←→  "2026 PLOT STAGE"
```

한국 전시·공연 제목은 **시리즈 접두어가 길어서** 서로 다른 프로그램의
bigram 유사도가 쉽게 0.6을 넘는다. 게다가 같은 소스 안에서는 이미 고유 id로
중복이 제거돼 있어 비교할 이유가 없었다.

→ 다른 소스끼리만 + 기간 겹침 + 시작일 14일 이내 +
(유사도 0.95 또는 0.85&같은장소). 현재 오판 0건.

> **교훈**: 데이터를 지우는 로직은 느슨하면 안 된다. 기준을 바꾸면 반드시 오삭제를 검증한다.

### 3.5 약한 지오코더의 실패가 강한 지오코더를 영구 차단

Nominatim이 실패한 질의를 `null`로 캐시해뒀더니, 나중에 네이버 지오코딩을 붙여도
캐시에 걸려 **호출 자체를 하지 않았다.** `서울 종로구 화동 106-5`가 네이버로는
즉시 찾아지는데 계속 "좌표 없음"으로 처리됐다.

→ 캐시에 지오코더 식별자(`by`)를 기록하고, 실패 캐시는 같은 지오코더일 때만 건너뛴다.

### 3.6 잘못된 URL이 쓰레기 데이터로 등록됨

존재하지 않는 전시 URL을 넣었더니 전용 어댑터가 실패한 뒤 범용 추출로 폴백해
사이트 공통 `og:title`인 `ARTMAP | Art is Everywhere`를 전시명으로 집었다.
미리보기 없이 실행했다면 그대로 커밋되어 지도를 오염시켰을 것이다.

→ 전용 어댑터가 있는 사이트에서 실패하면 폴백 금지 + 제목·기간·위치가 없으면 저장 거부.

### 3.7 추출 실패 시 직접 입력이 원천 봉쇄

`import-link.mjs`가 **추출 → 실패하면 즉시 종료 → (도달 못함) 수동값 적용** 순서였다.
직접 입력란을 만들어놓고, 정작 추출이 실패하면 그 값을 써보지도 못하고 죽었다.
아트맵에 없는 전시를 손으로 넣는 경로가 막혀 있었던 것.

→ 추출 실패를 종료 사유가 아니라 **"수동값으로 채울 기회"**로 변경.
URL 없이 제목만으로도 등록 가능하게 했다.

---

## 4. 성능

### 전시 추가 소요 시간 (실측)

| 단계 | 시간 | 단축 가능 |
|---|---|---|
| 워크플로 실행 | 13~22초 | ✅ 8초 단축함 (불필요한 Node 설치·npm 제거) |
| GitHub Pages 빌드 | 35~40초 | ❌ 유료 플랜도 동일 |
| CDN 전파 | 10~30초 | ❌ |

Pages 빌드 40초는 무료 정적 호스팅의 고정 비용이라 줄일 수 없다.
대신 관리자 목록을 `raw.githubusercontent`에서 읽어 **커밋 즉시 갱신**되게 하고,
실제 배포본을 확인해 "지도에 반영되었습니다 (n초)"로 알리도록 해 체감을 개선했다.

여러 건을 넣을 때는 배포를 기다리지 말고 **연달아 던져놓으면 된다.**

### 데이터 규모

```
1,159건 · 좌표 87.1%
  공연 675 · 전시 317 · 교육 111 · 축제 56
  공공데이터 1,130 · art-map 28 · 직접추가 1
```

좌표가 없는 약 150건은 대부분 **주소도 장소명도 없고 시도명만 있는**
상설 프로그램(`2026년 체험교육 프로그램` 등)이라 지오코딩으로도 찾을 수 없다.
분야별로는 교육·축제에 몰려 있고, **전시는 거의 전부 좌표를 가지고 있어**
이 앱의 주 용도에는 영향이 없다.

---

## 5. 검토했으나 채택하지 않은 것

| 안 | 이유 |
|---|---|
| 브라우저에서 공공데이터 API 직접 호출 | CORS 차단 + 서비스키 노출 |
| 실시간 API 조회 | 느리고 일일 호출 한도 소진 |
| 매일 art-map 크롤링 | 150요청/13건. 상업 사이트 부담 |
| 미술관 자체 홈페이지 범용 추출 | 디자인 위주라 실패율이 높고 사이트 개편에 취약 |
| Cloudflare Pages / Vercel 이전 | 배포는 5~10초로 빨라지나 GitHub 안에서 끝나는 단순함을 잃음 |
| 서버리스 함수(관리자 입력용) | 별도 인프라. GitHub API 직접 호출로 충분 |

---

## 6. 다음에 할 만한 것

- **오픈갤러리 일괄 수집** — 어댑터는 이미 있음. 목록 페이지 구조만 확인하면 됨.
  진행중 전시가 art-map보다 많을 가능성이 있어 우선순위 높음.
- **서울 열린데이터광장** — 서울 전시 밀도 보강 (현재 전시 317건)
- **커뮤니티 기능** — 사용자 후기·사진 (안양 프로젝트의 Firestore 방식)
- **미술관 어댑터 추가** — 자주 쓰는 곳이 생기면. 단 사이트 개편에 취약함을 감수.

---

## 7. 작업 방식 메모

- 코드 수정 전 **`AGENTS.md` 먼저 읽기.** 이미 알아낸 것을 다시 조사하지 않기 위함.
- 새 작업은 **새 대화 세션**에서 시작하는 편이 낫다.
  긴 세션은 전체 기록을 계속 들고 다녀 비용이 커진다.
- 추측으로 고치기 전에 **상태를 출력**한다. 이번 프로젝트에서 가장 비쌌던 실수가
  진단 없이 세 번 추측한 것이었다.
